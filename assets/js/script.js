// Function to reset the form and clear the displayed information
function resetForm() {
    $("#postcode").val(""); // Clear the postcode input field
    $("#postcode_info").empty(); // Clear the postcode information displayed on the page
}
